overall:
  agent_name: "random"
  output:
    verbose: false
    render: true
    save_timesteps: true
  episodes:
    max: 600
  timesteps:
    max: 200
    start_training: 50
agents:
  random:
  random2:

